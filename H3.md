# Apache

## Install Apache
``` bash
sudo apt-get install apache2 -y
```

First thing I want to do is delete the default website

``` bash
sudo systemctl reload apache2
cd /var/www/html
sudo rm index.html
```

cd /etc/apache2/sites-available
sudoedit hometest.site
sudo rm /etc/apache2/sites-available/000-default.conf

---
- Make new site
``` bash
sudoedit hometest.site
```
- cat hometest.site
``` yml
<VirtualHost *:80>
    ServerName hometest.site
    DocumentRoot /home/azian/sites

    <Directory /home/azian/sites/>
        Require all granted
    </Directory>
</VirtualHost>
```

---
```bash
cd
mkdir sites
cd sites
nano index.html
```

cat index.html
Hello there!

sudo systemctl restart apache2
```

My text isn't showing.

Checked logs:

``` bash
sudo less /var/log/apache2/error.log
```

[Fri Apr 10 14:43:04.829472 2026] [mpm_event:notice] [pid 1090433] AH00489: Apache/2.4.58 (Ubuntu) configured -- resuming normal operations  
[Fri Apr 10 14:43:04.829544 2026] [core:notice] [pid 1090433] AH00094: Command line: '/usr/sbin/apache2'

No error.

Checked my config.

I forgot to run:

cd /etc/apache2/sites-enabled
sudo a2ensite hometest.site

Got error:

ERROR: Site hometest.site does not exist!

I forgot that you need to have .conf behind hometest.site

---

## Fix

cd /etc/apache2/sites-available
sudoedit hometest.site.conf

cat hometest.site.conf

<VirtualHost *:80>
    ServerName hometest.site
    DocumentRoot /home/azian/sites

    <Directory /home/azian/sites/>
        Require all granted
    </Directory>
</VirtualHost>

``` bash
sudo systemctl reload apache2
```

I needed to set permissions:

cd ~/sites
sudo chmod ugo+x .

sudo systemctl reload apache2

Now it works.

---

## Add real HTML

nano index.html

cat index.html

<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Havoc landing</title>
</head>

<body style="margin:0;height:100vh;display:flex;justify-content:center;align-items:center;background:#ffe4d6;">

<div style="position:absolute;width:200px;height:200px;border:2px solid #ff6a3d33;border-radius:50%;top:-50px;left:-50px;"></div>

<div style="position:absolute;width:300px;height:300px;border:2px solid #ff6a3d22;border-radius:50%;bottom:-80px;right:-80px;"></div>

<button style="padding:15px 35px;border:none;border-radius:30px;background:#ff4d1a;color:white;font-size:16px;box-shadow:0 10px 25px #ff4d1a55;">
Whoop
</button>

</body>
</html>

``` bash
sudo systemctl reload apache2
```
