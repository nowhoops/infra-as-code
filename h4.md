
```
#     #    #     #
#     #    #     #
#     #    #     #
#######    #######
#     #          #
#     #          #
#     #          #

```
(karvinen, 2026a)

--- 

| core | ram | disk  | vm/os          |
|------|-----|-------|----------------|
| 4    | 8   | 100GB | Debian Trixie  |
| 12    | 4   | 60GB  | Debian 13  |
VM platform used for this was: TrueNAS

# A) Manual

## Context

Our teacher said that it is odd that i haven't been able to setup unifi on Debian. So naturally I need to try again. I tried installing everything manually on Debian using multiple guides. That failed repeatedly, mostly because the guides were outdated or inconsistent.

Even the official UniFi documentation was outdated.

---

## Initial Manual Installation Attempts

I tried installing UniFi controller directly on Debian.

Problems encountered:

- Failed to start mongod
- Issues with openjdk-17-jre
- UniFi being extremely sensitive to package versions
- MongoDB not starting properly
- General dependency mismatch issues

Most of the time was spent trying to find up-to-date guides.

Couldn't find anyting broken in my setup.

---

## Reddit saves the day

Eventually I found a Reddit guide that actually worked (few months old) :

https://www.reddit.com/r/Ubiquiti/comments/1qgscs1/how_to_guide_installing_unifi_network_application/

The Difference: correct MongoDB repo + correct UniFi repo setup.

### I ran the guide step by step (copy from from Reddit)

```
# Update system
sudo apt update && sudo apt upgrade -y && sudo apt autoremove -y

# Required tools
sudo apt install -y gnupg curl ca-certificates

# Keyring directory
sudo install -d -m 0755 /etc/apt/keyrings

# MongoDB key
curl -fsSL https://www.mongodb.org/static/pgp/server-8.0.asc | sudo gpg --dearmor -o /etc/apt/keyrings/mongodb-server-8.0.gpg
sudo chmod 0644 /etc/apt/keyrings/mongodb-server-8.0.gpg

# MongoDB repo
sudo tee /etc/apt/sources.list.d/mongodb-org-8.0.sources > /dev/null <<'EOF'
Types: deb
URIs: https://repo.mongodb.org/apt/debian
Suites: bookworm/mongodb-org/8.0
Components: main
Architectures: amd64
Signed-By: /etc/apt/keyrings/mongodb-server-8.0.gpg
Enabled: true
EOF

# UniFi key
curl -fsSL https://dl.ui.com/unifi/unifi-repo.gpg | sudo tee /etc/apt/keyrings/unifi-repo.gpg > /dev/null
sudo chmod 0644 /etc/apt/keyrings/unifi-repo.gpg

# UniFi repo
sudo tee /etc/apt/sources.list.d/unifi.sources > /dev/null <<'EOF'
Types: deb
URIs: https://www.ui.com/downloads/unifi/debian
Suites: stable
Components: ubiquiti
Architectures: amd64
Signed-By: /etc/apt/keyrings/unifi-repo.gpg
Enabled: true
EOF

# Install
sudo apt update
sudo apt install -y mongodb-org
sudo apt install -y unifi
```
(Reddit,
eW4GJMqscYtbBkw9)


After that:

```
systemctl status unifi
```

Service showed as running.
```
Loaded: loaded (/usr/lib/systemd/system/unifi.service; enabled; preset: enabled)
```


---
### Edit
#### I Critical Mistake

At this point I assumed everything was working because:

```
Loaded: loaded (/usr/lib/systemd/system/unifi.service; enabled; preset: enabled)
```

So I thought it was fine.
I deleted the vm and popped new one.

Mistake: I never actually checked the web UI.

So the service was “running” but not actually usable.

Lesson: systemctl status is not enough. Always verify service functionality.
### End edit

---

## B) Ansible Attempt 



Structure:

```
- hosts: all
  become: true
  roles:
    - unifi
```

---

## Ansible Role Breakdown
- I ran these one block at a time. There were a few typos and formatting issues along the way, but I’ve left those out of the report since they don’t really add anything useful.
- ✔️ = I ran the command ansible-playbook unifi.yml successfully.

### 1. Install required packages

```
- name: Install required packages
  apt:
    name:
      - gnupg
      - curl
      - ca-certificates
    state: present
```
✔️
---

### 2. Create keyring directory

```
- name: Create keyring directory
  file:
    path: /etc/apt/keyrings
    state: directory
    mode: "0755"
```
✔️
---

### 3. Download MongoDB key

```
- name: Download MongoDB GPG key
  get_url:
    url: https://www.mongodb.org/static/pgp/server-8.0.asc
    dest: /tmp/mongodb-server-8.0.asc
    mode: "0644"
```
✔️
---

### 4. Convert key + add repo

```
- name: Convert MongoDB key to keyring format
  command: gpg --dearmor -o /etc/apt/keyrings/mongodb-server-8.0.gpg /tmp/mongodb-server-8.0.asc
  args:
    creates: /etc/apt/keyrings/mongodb-server-8.0.gpg

- name: Add MongoDB repository
  copy:
    dest: /etc/apt/sources.list.d/mongodb-org-8.0.sources
    mode: "0644"
    content: |
      Types: deb
      URIs: https://repo.mongodb.org/apt/debian
      Suites: bookworm/mongodb-org/8.0
      Components: main
      Architectures: amd64
      Signed-By: /etc/apt/keyrings/mongodb-server-8.0.gpg
      Enabled: true
```
✔️
---

### 5. UniFi repo setup

```
- name: Download UniFi GPG key
  get_url:
    url: https://dl.ui.com/unifi/unifi-repo.gpg
    dest: /etc/apt/keyrings/unifi-repo.gpg
    mode: "0644"

- name: Add UniFi repository
  copy:
    dest: /etc/apt/sources.list.d/unifi.sources
    mode: "0644"
    content: |
      Types: deb
      URIs: https://www.ui.com/downloads/unifi/debian
      Suites: stable
      Components: ubiquiti
      Architectures: amd64
      Signed-By: /etc/apt/keyrings/unifi-repo.gpg
      Enabled: true
```
✔️
---

### 6. Install packages

```
- name: Update apt cache
  apt:
    update_cache: yes

- name: Install MongoDB and UniFi
  apt:
    name:
      - mongodb-org
      - unifi
    state: present
```
✔️
---

## Problem After Ansible Install

UniFi service  doesn't work like it should

Checked:

```
systemctl status unifi
```

Service appeared active, but UI was not accessible.

---
##### (Made the edit on top)
## MongoDB Issue

Checked logs:

```
tail -n 50 /usr/lib/unifi/logs/mongod.log
```
<img width="1342" height="736" alt="Screenshot From 2026-04-16 19-40-08" src="https://github.com/user-attachments/assets/7130cff4-109a-46ff-9c85-980e87379a11" />

Output:

- Unable to delete repair file
- Mongo log not found
- Database process stopped, code=132

---

### (Error 132)

Alot of people have had this problem but I didn't get good reason. Best help that lead me to the correct path

MongoDB error 132 = Illegal Instruction crash.

Source discussion:
https://www.mongodb.com/community/forums/t/mongodb-immediately-exits-with-error-132-and-will-not-ressurect/152513

---
It might be caused by hardware incompatibility.
This gave me an idea.

### Hypothesis

The Problem might be caused by:

- VM CPU mode is virtual.

### My problem with this Hypothesis

- Same cpu mode works on ubuntu server.
- I really don't want to use 20mins setting up fresh debian.
- 
---

### Fresh Debian

Created new VM with CPU passthrough with mode.

Then:

- Reinstalled Debian
- Re-ran Ansible playbook

```
ansible-playbook unifi.yml
```
Works 
✔️
<img width="876" height="65" alt="Screenshot From 2026-04-17 20-48-08" src="https://github.com/user-attachments/assets/3fa975bd-2d1f-4277-881e-5cbfa0de1890" />


<img width="1092" height="485" alt="Screenshot From 2026-04-17 20-49-33" src="https://github.com/user-attachments/assets/74f31b87-5bcd-4741-978c-0428aff5a376" />

---

## C) Config Testing

#### Config:

```
cat ansible.cfg
[defaults]
inventory = hosts.ini
```
#### Test:

```
ansible all -m ping
```
<img width="513" height="86" alt="Screenshot From 2026-04-17 23-08-56" src="https://github.com/user-attachments/assets/3ecd6374-5ff0-4dee-ae87-8bc32eb31e24" />


#### Changes:

```
cat ansible.cfg
[defaults]
inventory = nohosts.ini
```

#### Test:

```
ansible all -m ping
```

<img width="1248" height="59" alt="Screenshot From 2026-04-17 23-11-16" src="https://github.com/user-attachments/assets/d78cbe41-f284-4537-b4c9-f09586edccbb" />




---

## D) Cleanup Test

On Debian VM:

```
sudo apt-get purge -y unifi
```

<img width="998" height="344" alt="Screenshot From 2026-04-17 23-28-31" src="https://github.com/user-attachments/assets/2cc0fa0f-be72-4848-820f-b9e5f95ce57b" />
#### Checked the web ui
<img width="886" height="323" alt="Screenshot From 2026-04-17 23-31-41" src="https://github.com/user-attachments/assets/1e45431f-b26d-4132-9d3c-9a6036d4eaeb" />

#### Then re-ran:

```
ansible-playbook unifi.yml
```
<img width="1161" height="121" alt="Screenshot From 2026-04-17 23-32-14" src="https://github.com/user-attachments/assets/a0432cf2-d897-42b4-955a-35d043312af7" />

Tested idempotency by running playbook again.
<img width="1161" height="121" alt="Screenshot From 2026-04-17 23-33-00" src="https://github.com/user-attachments/assets/77ca1abd-5909-4a61-9407-e2f28590d216" />

---
